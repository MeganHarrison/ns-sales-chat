{
  "name": "r2-image-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for managing images in R2 bucket",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "keywords": ["cloudflare", "worker", "r2", "images"],
  "author": "Nutrition Solutions",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}