{
  "name": "sync-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Keap-Supabase sync operations",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.30",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.98.0"
  }
}